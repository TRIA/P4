stages:
  - build
  - test

job_efcp_build:
  script:
    - cd dataplane/p4
    - make efcp-build
  stage: build
  tags: [npl_bmodel]

job_cp_test:
  script:
    - sudo -S su
    - cd controlplane/p4
    - make edf-cp-test-stratum
    - cat test/edf-dp-server.log
    - cat test/edf-cp-client.log
  dependencies:
    - job_efcp_build
  stage: test
  tags: [npl_bmodel]
